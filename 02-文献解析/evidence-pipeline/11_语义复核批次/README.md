# 语义复核批次

本目录保存逐来源人工语义复核过程。每个批次包含：

- `batch_manifest.csv`：来源范围、处理状态和复核人；
- `packets/`：将该来源目录中的全部分析资产按文件边界合并，供连续阅读；
- `source_cards/`：完成语义复核后的正式来源卡；
- `mappings/`：经复核准入的A—D／X章—节映射；
- `audit.md`：批次覆盖、版本、冲突和证据边界审计。

`packets/`只改变阅读组织方式，不修改原报告。完成包文件阅读不等于完成原文核验；未返回clean原文时，核验等级最高为V2。

