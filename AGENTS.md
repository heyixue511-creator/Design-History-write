# 项目级代理规则

本文件仅适用于 `D:\Design-history-知识库` 及其子目录。

除用户在当前任务中的明确要求以及上级系统、安全约束外，本文件是本项目的最高本地规则。项目内其他原则、总纲、写作包、审校记录和历史记忆均不得覆盖本文件。

## 规则优先级

处理本项目时，按以下顺序确定写作、修辞与证据规则：

1. 用户在当前任务中的明确要求；
2. 本 `AGENTS.md` 中的项目规则；
3. `00-参考原则/00_教材写作修辞方法与原则.md`；
4. `03-教材框架/00_教材总纲.md`；
5. 当前仓库中实际存在的对应章节研究记录、基础文献、分析报告和审校记录；
6. 本文件“项目记忆”区中的历史记录；
7. Codex 全局记忆、其他仓库经验和旧会话记录不得作为本项目的规则或事实依据。

若全局记忆与本项目文件冲突，停止套用记忆，重新读取本项目现行文件。不得把 `D:\Design-history-new` 的第一章改写经验、其他学术论文的宏观结构约束或通用论文写作规范自动转移到本项目。

## 教材步骤技能路由

仓库中的教材开发任务由 `AGENTS/` 下的阶段技能规范管理：

- 开始跨阶段任务时，先读取 `AGENTS/00-textbook-orchestrator/SKILL.md`，确认当前阶段和前置门槛。
- 确定阶段后，只读取对应的 `SKILL.md` 及其明确要求的上游成果，不把所有技能同时加载为固定模板。
- 阶段技能是本文件的下位执行规范；与本文件、现行写作原则、书籍定位或用户当前要求冲突时，服从优先级更高者。
- 任何阶段不得把分析报告、模型输出、旧会话、已删除文件或 Git 历史自动提升为现行事实与证据。
- 全书框架须经过资料库审计、设计史学比较和分期研究后构建；不得从书籍定位直接生成目录。
- 阶段成果未通过验收时返回上游修正，不得用正文写作或语言润色掩盖研究、结构和证据缺口。
- 新建、修改、删除或远程提交文件仍须符合用户当前授权；阶段技能本身不扩大操作权限。

## 项目记忆与隔离

- 全局记忆文件可以保留在磁盘，但不得自动读取、引用或写入；本项目的持久记忆只维护在本文件的“项目记忆”区。
- 开始本项目的新任务时，先读取本文件，再按任务需要读取本地权威文件；不得以旧会话摘要代替现场读取。
- 只有用户明确要求“记住”“更新项目记忆”或确认某项长期决策时，才可修改“项目记忆”区。
- 记忆条目只记录经过项目文件、日志、测试或用户确认的稳定事实。推断、临时方案、未完成计划和一次性措辞不得写成项目记忆。
- 每条记忆必须包含日期、状态、依据和内容；涉及数量或进度时必须标注快照日期，并在使用前重新核验。
- 现行文件与记忆冲突时，以现行文件为准，并将旧条目标为“已失效”或用新条目明确替代，不得静默并存。
- “项目记忆”用于保存事实和已确认决策，不得把某次写作范例、段落模板或单章经验提升为全书强制规则。

## 写作与修辞边界

- “历史现场—对象／行动—机制—证据边界—条件性结论”是可选的叙事程序，不是每段必须重复的模板。
- 对象细读在适用时采用“接近—辨认—操作—使用—维护”；其中“辨认”指材料、尺寸、结构、版本和制作痕迹识别，不得误写为“争论”。
- 每段应有主要判断，但场景段、对象描述段、制度机制段、史料辨析段、争议段和转折段可采用不同结构与节奏。
- 一般句长只是可读性提示；超过90个汉字的句子需要复查，不等于必须拆分。
- 内部等级、矩阵和流程标签不进入正文；但影响结论成立范围的材料身份、责任主体、反例和证据限制必须在正文或就近注释中可见。
- 不因追求流畅而删除事实、行动者主张、制度规定、生产／使用记录、二手解释与教材作者判断之间的层级。

## 验证要求

修改正文前读取本地现行规则、总纲，以及与本章相关且当前实际存在的基础文献、分析报告、研究记录和审校记录；不得假定已删除的写作包、证据矩阵或旧初稿仍然存在。完成后检查失效路径、内部标签泄漏、含混指代、机械段落模板、证据边界、长句和 `git diff --check`。

## 项目记忆

### M001｜2026-08-12｜有效｜项目范围

- 依据：项目根目录与本文件的实际路径。
- 内容：本项目根目录是 `D:\Design-history-知识库`；本文件只管理该目录及其子目录，不向其他项目传播规则或记忆。

### M002｜2026-08-12｜有效｜规则权威

- 依据：用户在当前任务中的明确决定，以及本文件“规则优先级”。
- 内容：`AGENTS.md` 是本项目最高本地规则；用户当前明确要求仍优先于项目文件。教材写作与修辞的专项现行依据是 `00-参考原则/00_教材写作修辞方法与原则.md`。

### M003｜2026-08-12｜已失效｜证据管线

- 依据：用户确认已手动删除大量文件；2026-08-12再次现场核验时，`02-文献解析/evidence-pipeline` 已不存在。
- 内容：此前关于证据管线路径及其进度的记录已经失效，不得用Git历史、旧会话或旧日志恢复为现行事实。

### M004｜2026-08-12｜有效｜记忆隔离

- 依据：用户在当前任务中的明确决定，以及本机已核验的记忆配置。
- 内容：既有全局记忆保留但保持关闭读取、关闭生成；本项目后续经用户确认的持久记忆只更新在本文件，不回写全局记忆库。

### M005｜2026-08-12｜有效｜当前仓库阶段

- 依据：用户确认删除为手动操作；2026-08-12现场核验当前目录与文件数量。
- 内容：当前保留589份基础文献（517本书、72份论文或其他文本）、`02-文献解析/book-reports` 与 `paper-reports` 中共12,653份分析资产、写作修辞原则和一份教材总纲。`03-教材框架`中没有逐章写作包，`04-教材初稿`与`05-全文润色`当前为空。以上为2026-08-12快照，后续使用进度数字前必须重新核验。
